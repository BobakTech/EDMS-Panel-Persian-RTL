/**
 * ============================================================================
 * Application Constants
 * ----------------------------------------------------------------------------
 * Defines shared application-wide constants.
 * ============================================================================
 */

export const APP_NAME = "EDMS";

export const DEFAULT_LANGUAGE = "fa";

/**
 * Default to dark mode for the frontend demo.
 * Switch this back to light when integrating with the PHP panel.
 */

export const DEFAULT_THEME = "dark" as const;
